# Database Configuration
DB_HOST=db
DB_NAME=sffaiz
DB_USERNAME=sffaiz
DB_PASSWORD=sffaiz-pass
DB_ROOT_PASSWORD=root-password

# Email Configuration
EMAIL_ADMIN=admin@sfjamaat.org
EMAIL_CONTACT=fmb@sfjamaat.org
EMAIL_SECRETARY=secretary@sfjamaat.org

# External Links
LINK_PLANNING=https://form.jotform.com/222676794136163
LINK_FEEDBACK=https://goo.gl/forms/PTUd1L9ASeVIeNB92

# Application Settings
APP_NAME="TEST FAIZ"
SECRETARY_TITLE="Test Secretary"

# RSVP Cutoff Configuration
# Cutoff mode: "daily" or "weekly"
# - "daily": Users can RSVP until a specific time each day for the next day(s)
# - "weekly": Users can RSVP until a specific day/time each week for the upcoming week
RSVP_CUTOFF_MODE="daily"

# Timezone for all date/time calculations (PHP timezone identifier)
# Examples: America/Los_Angeles, America/New_York, America/Chicago
# See: https://www.php.net/manual/en/timezones.php
RSVP_TIMEZONE=America/Los_Angeles

# Weekly cutoff settings (used when RSVP_CUTOFF_MODE=weekly)
# Day of week when RSVP cutoff occurs (Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday)
RSVP_WEEKLY_CUTOFF_DAY=Thursday
# Time on cutoff day when RSVPs lock (24-hour format HH:MM, e.g., 23:00 = 11:00 PM)
RSVP_WEEKLY_CUTOFF_TIME=23:00
# First day of the meal week (Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday)
RSVP_WEEKLY_WEEK_START=Monday

# Daily cutoff settings (used when RSVP_CUTOFF_MODE=daily)
# Time each day when RSVPs lock for upcoming days (24-hour format HH:MM, e.g., 21:00 = 9:00 PM)
RSVP_DAILY_CUTOFF_TIME=21:00
# How many days in advance the cutoff applies
# Example: advance_days=1 with 9pm cutoff means:
#   - Before 9pm today: can edit tomorrow onwards
#   - After 9pm today: can only edit day after tomorrow onwards (tomorrow is locked)
RSVP_DAILY_ADVANCE_DAYS=1

# Size Selection Configuration
# Available thaali sizes in your implementation (comma-separated, smallest to largest)
# Examples: "XS,S,M,L,XL" (full range) or "S,M,L" (limited range)
# IMPORTANT: Sizes must be listed in order from smallest to largest
THAALI_SIZES=XS,S,M,L,XL

# Controls which thaali sizes users can select when making RSVPs
# Options:
# - "any": Users can select any size from THAALI_SIZES
# - "downgrade-only": Users can only select sizes smaller than or equal to their default size
# - "plus-minus-one": Users can select one size up or down from their default size
#   Examples: Default M → can select S,M,L | Default S → can select XS,S,M | Default XL → can select L,XL
# Note: Admins can always select any size regardless of this setting
SIZE_SELECTION_MODE=plus-minus-one
